// Note: Rubric input might be integrated directly into QuestionForm.jsx
// This file is a placeholder if a separate component is desired.

function RubricForm() {
  return (
    <div>
      <p>TODO: Create a form specifically for adding/editing rubrics with LaTeX input.</p>
      <p>TODO: Consider integrating this into QuestionForm.jsx for simplicity.</p>
    </div>
  );
}

export default RubricForm;